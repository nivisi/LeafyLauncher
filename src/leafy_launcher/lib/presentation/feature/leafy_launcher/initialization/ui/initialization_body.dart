import 'package:flutter/widgets.dart';

class InitializationBody extends StatelessWidget {
  const InitializationBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Loading ...'),
    );
  }
}
