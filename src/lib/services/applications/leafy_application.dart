import 'application.dart';

class LeafyApplication extends Application {
  const LeafyApplication({
    required String name,
    required String package,
  }) : super(name: name, package: package);
}
