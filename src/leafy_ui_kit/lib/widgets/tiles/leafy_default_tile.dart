import 'package:flutter/material.dart';

class LeafyDefaultTile extends StatelessWidget {
  const LeafyDefaultTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
